pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{ts,tsx,js,json,css}"
      run: bunx biome check --write {staged_files}
      stage_fixed: true
    typecheck:
      run: bunx tsc --noEmit
